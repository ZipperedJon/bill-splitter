# Normalise to LF in the repository. This matters most for install.sh: a shell
# script checked out with CRLF fails on the Pi with
#   /usr/bin/env: 'bash\r': No such file or directory
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
*.db binary
